# Contributing

Install Node.js 18+, run `npm install`, then `npm run typecheck`, `npm test`, and
`npm run format:check`. Keep analyzers deterministic, avoid network calls during
tests, and add a fixture/test for new behavior.
