{
  "name": "repory",
  "version": "0.1.0",
  "description": "Understand your codebase",
  "author": "7Myo",
  "license": "MIT",
  "type": "module",
  "bin": {"repory": "dist/src/cli.js"},
  "files": ["dist", "README.md", "LICENSE", "docs"],
  "scripts": {"build": "tsc", "prepare": "npm run build", "prepack": "npm run build", "dev": "npm run build && node dist/src/cli.js", "start": "npm run build && node dist/src/cli.js", "repory": "npm run build && node dist/src/cli.js", "typecheck": "tsc --noEmit", "test": "npm run build && node --test dist/tests/*.test.js", "format": "prettier --write src tests", "format:check": "prettier --check src tests"},
  "engines": {"node": ">=18"},
  "dependencies": {"@types/node": "^22.0.0", "typescript": "^5.6.0"},
  "devDependencies": {"prettier": "^3.3.3"}
}
